# Contracts: Reorder Payment Form Fields

No external interface contracts are modified by this feature. The payment form's HTTP endpoint (`/payments/create/`) remains unchanged — only the field rendering order within the HTML response is affected.
