# Implementation Plan: Reports Menu Option

**Branch**: `024-reports-menu` | **Date**: 2026-06-24 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `specs/024-reports-menu/spec.md`

## Summary

Add a "Reports" dropdown menu to the main navigation bar with a "Payments" sub-item linking to `/payments/reports/`, visible only to administrators. This matches the existing view-level permission check on `PaymentReportView` (superuser or Administrators group membership).

## Technical Context

**Language/Version**: Python 3.12+, Django 5.0.x

**Primary Dependencies**: Django 5.0 (django.contrib.auth, Bootstrap 5 CSS/JS bundled via CDN in base template)

**Storage**: N/A — no new data storage required

**Testing**: Django TestCase (existing project pattern), pytest

**Target Platform**: Docker container (Linux), served via gunicorn behind whitenoise

**Project Type**: Web application (Django monolith)

**Performance Goals**: N/A — menu rendering adds negligible overhead

**Constraints**: N/A

**Scale/Scope**: Single change to one template file (`base.html`), one i18n translation entry

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Gate | Status | Notes |
|------|--------|-------|
| **I. Code Quality** | ✅ PASS | Minor template addition; no lint violations expected |
| **II. Testing Standards (TDD)** | ✅ PASS | Tests must be written and reviewed before implementation; Red-Green-Refactor cycle |
| **III. UX Consistency (i18n)** | ✅ PASS | "Reports" and "Payments" labels MUST be internationalized via `{% translate %}`; Spanish translations required in `django.po` |
| **IV. Performance** | ✅ PASS | No performance impact; menu rendering adds negligible load |
| **Development Workflow** | ✅ PASS | Sequential branch `024-`; atomic commit; independent user story as MVP |

No violations found. Complexity Tracking section omitted.

## Project Structure

### Documentation (this feature)

```text
specs/024-reports-menu/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
├── contracts/           # Phase 1 output (empty — no external interfaces)
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
backend/
├── apps/
│   └── payments/
│       ├── urls.py                     # Has payments:reports route
│       └── views.py                    # Has PaymentReportView (already exists)
├── templates/
│   └── base.html                       # TARGET: Add Reports > Payments dropdown
├── config/
│   └── urls.py                         # Root URL config (no change needed)
└── locale/
    └── es/LC_MESSAGES/
        └── django.po                   # TARGET: Add "Reports" and "Payments" translations

backend/tests/
├── test_payments.py                    # TARGET: Add tests for Reports menu visibility
```

**Structure Decision**: Django monolith with apps layout. The change is contained to `backend/templates/base.html` (navbar), `backend/locale/es/LC_MESSAGES/django.po` (i18n), and test files.
