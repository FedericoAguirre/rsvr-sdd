# Data Model: Add Clients Import Button

**Status**: N/A — This feature introduces no new data entities, fields, or state transitions.

## Rationale

The feature is a pure frontend navigation change: add a "Subir Clientes" link on the existing `/clients/search/` page that points to the existing `/clients/upload/` page. No database changes, no new models, no new API endpoints.

Refer to the [feature spec](spec.md) and the existing [CSV client upload data model](../016-csv-client-upload/data-model.md) for the Client entity.
