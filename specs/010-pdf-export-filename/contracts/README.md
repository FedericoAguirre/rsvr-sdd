# Contracts: Rename Exported Reservations PDF

**Status**: N/A — No external interface contracts changed.

The HTTP endpoint URL, query parameters, request method, and response content type are all unchanged. Only the filename value in the Content-Disposition header differs.
