# Data Model: Rename Exported Reservations PDF

**Status**: N/A — No data model changes.

This feature modifies only the runtime filename string in the
`Content-Disposition` HTTP response header. No entities, fields,
relationships, or validation rules are added, removed, or altered.
