# Contracts: Remove Docker for Web Development

No external interface contracts are modified by this feature. The project's interfaces (HTTP API, Django admin, etc.) are unchanged — only the development environment configuration is altered.
