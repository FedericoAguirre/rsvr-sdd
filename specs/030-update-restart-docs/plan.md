# Implementation Plan: Update Restart Docs

**Branch**: `030-update-restart-docs` | **Date**: 2026-07-06 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/030-update-restart-docs/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command.

## Summary

Update `docs/windows11_deployment.md` Option 3 (and align Options 1 & 2) to use a PowerShell-based `.env` loader followed by `uv run .\manage.py runserver` instead of the current `cmd.exe` / `waitress-serve` approach. Add a test in `backend/tests/` that validates the PowerShell command syntax via static analysis (no execution).

## Technical Context

**Language/Version**: Python 3.12+ (Django), PowerShell 5.1+ (Windows)

**Primary Dependencies**: uv (Python package & project manager), Windows Task Scheduler, PowerShell

**Storage**: N/A — documentation change only

**Testing**: pytest via Django test runner; static analysis of PowerShell command strings extracted from markdown

**Target Platform**: Windows 11 Home (no Pro/Enterprise features); docs rendered on GitHub

**Project Type**: Django web application + deployment documentation

**Performance Goals**: N/A

**Constraints**: Windows 11 Home cannot use Group Policy or secpol.msc; PowerShell must use `-Command` flag for inline scripts; project path is `D:\Descargas\codigo\rsvr-sdd`

**Scale/Scope**: Single markdown file update (`docs/windows11_deployment.md`) + one test file (`backend/tests/`)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### Gate 1 — Testing Standards (NON-NEGOTIABLE)
- **Requirement**: TDD — tests must be written before implementation and must fail first
- **Assessment**: PASS — FR-007 requires a test method. Tests will validate PowerShell command syntax statically (no execution). TDD cycle applies: write failing test → implement doc changes → tests pass
- **Justification if violated**: N/A — will comply

### Gate 2 — i18n (NON-NEGOTIABLE)
- **Requirement**: Every user-visible string must be internationalized
- **Assessment**: PASS — this feature modifies deployment documentation written in English for developers. The documentation is a technical reference guide, not application UI. i18n does not apply to developer-facing docs in this project context
- **Justification if violated**: N/A — no user-facing UI strings added

### Gate 3 — Code Quality
- **Requirement**: All code must pass linting and static analysis
- **Assessment**: PASS — no application code changes; only documentation and test code. Test code will pass linting

### Gate 4 — Performance Requirements
- **Requirement**: Feature must define measurable performance criteria
- **Assessment**: PASS — SC-004 defines a 10-minute setup time metric. N/A for documentation change otherwise

## Project Structure

### Documentation (this feature)

```text
specs/030-update-restart-docs/
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
├── contracts/           # Phase 1 output
└── tasks.md             # Generated by /speckit.tasks
```

### Source Code (repository root)

```text
docs/
└── windows11_deployment.md        # MODIFIED — Option 1, 2, 3 updated

backend/
└── tests/
    └── test_restart_docs.py        # NEW — static analysis test for PowerShell commands
```

**Structure Decision**: Single-doc update in `docs/` + one new test file in `backend/tests/` matching existing test conventions. No application code changes needed.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

No violations to justify. Feature is a straightforward documentation update with one test file.
